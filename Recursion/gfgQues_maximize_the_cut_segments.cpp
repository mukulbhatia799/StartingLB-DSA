// The pattern used in this ques is called (exploring all possible ways).
// This Ques will give time limit exceed(TLE) while solving using recursion. This ques can be solved using dynamic programming in better time complexity and will not give tle.
#include<iostream>
using namespace std;

void maxCutSegments(int n, int x, int y, int z)
{
        // will do in next class.
}

int main(){
    int n;
    cin >> n;

    int x, y, z;
    cin >> x >> y >> z;

    maxCutSegments(n, x, y, z);

    return 0;
}